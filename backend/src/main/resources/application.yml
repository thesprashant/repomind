spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/repomind
    username: repomind
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
